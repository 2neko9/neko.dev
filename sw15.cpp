#include <iostream>
#include <cstdlib>

using namespace std;

int main()
{
    const double pi = 3.1416;
    int radius = 50;
    cout<<"C++ program that will compute the area of the cicle based on the given radius\n";
    cout<<"PI is equals to: "<<pi;
    int area = (radius*50)*pi;

    cout<<"\n\nThe area of the circle if the radius is 50: "<< area;
    return 0;
}
